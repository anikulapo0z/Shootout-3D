using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Bullet : MonoBehaviour
{
    /*
    public float damage = 10f; // Set this to the amount of damage you want the bullet to inflict

    //handle collision with other objects
    void OnCollisionEnter(Collision collision)
    {
        // Get the Health component from the object we collided with
        Health health = collision.gameObject.GetComponent<Health>();
        // If the object we collided with has a Health component, deal damage to it
        if (health != null)
        {
            health.ApplyDamage(damage);
        }
        else// If the object we collided with does not have a Health component do nothing
        {
            Debug.Log("Object hit does not have a Health script");
        }
        
        // Destroy the bullet
        Destroy(gameObject);
    }
    */

}
